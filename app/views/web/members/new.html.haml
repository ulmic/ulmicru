- title
.container
  %h3.title
    = t('.title')
  %p
    %b
      Привет!
  %p
    Если ты оказался на этой странице, значит хочешь официально присоединиться к команде Молодёжного инициативного центра.
  %p
    Оформляя эту заявку ты должен осознавать, МИЦ - это общественная организация. Вступая в неё каждый берёт на себя ответственность за её развитие: он должен участвовать в Областных Конференциях, выполнять обязанности, возложенные на него Уставом, в т.ч. заботиться об авторитете МИЦ, состоять на учёте в одном из местных отделений или структурных подразделений организации.
  %p
    Чтобы деятельность МИЦ была максимально прозрачной, мы прописали все принципы и основы деятельности в Уставе. Не забудь прочитать его ЗДЕСЬ, а ЗДЕСЬ узнать традиции нашей команды.
  %p
    Каждый наш активист может принимать участие в мероприятиях всех направлений: в центральных программах «Лидер», «Inформация», «Право», «Добро» и областных проекта «Информационные технологии» и «Диалог культур».
  %p
    Для поддержки самых активных представителей нашей команды, в МИЦ работает эффективная корпоративная политика. Ежегодно мы вручаем премию "Признание", проводим конкурс «Мистер и Мисс МИЦ», конкурс «Лучшая команда», выпускаем корпоративные тетради и мн.др.
  %p
    Если в периоде заполнения анкеты у тебя возникнут какие-то проблемы, то оперативно звони руководителю корпоративных проектов Марии Кирпичниковой по телефону 8-905-349-37-76, komanda@ulmic.ru. В течение недели после заполнения анкеты она  свяжется с тобой и ответит на все возникающие вопросы. Если этого не произошло, набери её сам. Телефон выше.
  %p
    Желаем удачи!
.container.mic-form-container
  = simple_form_for @member_form, url: members_path, method: :post do |f|
    - f.with_options required: true do |f|
      = f.input :first_name, as: :string
      = f.input :patronymic, as: :string
      = f.input :last_name, as: :string
      = f.input :email, as: :string
      = f.input :motto, as: :string
      = f.input :ticket, as: :string
      = f.input :mobile_phone, as: :string, placeholder: 'locale'
      = f.input :birth_date, as: :date_picker
      = f.input :home_adress, as: :string
      = f.input :municipality
      = f.input :locality, as: :grouped_select, collection: Localities.list_hash, group_method: :last
      = f.input :type, as: :hidden, input_html: { value: 'Member' }
      = f.input :state, as: :hidden, input_html: { value: :unviewed }
      = f.input :parent_id, as: :select, collection: members_hash(@active_members)
      = f.input :avatar, as: :file
    = f.button :submit, t('helpers.links.save'), class: 'btn-success'
    = link_to t('helpers.links.back'), root_path, class: 'btn btn-default'
