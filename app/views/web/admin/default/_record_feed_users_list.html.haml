= render 'web/admin/default/comment_form_button'
%ul.list-inline.users_mini_avatars
  - feeds.map(&:user).uniq.each do |f|
    %li
      = link_to show_path(f) do
        = image_tag f.avatar&.small
= render 'web/admin/default/comment_form_button'
