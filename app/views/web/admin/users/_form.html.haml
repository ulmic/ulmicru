.page-header
  %h1= page_title(action, User.model_name.human)
.row
  .col-lg-6
    = simple_form_for @user_form, url: { controller: 'web/admin/users', action: action }, input_html: { class: 'form-horizontal' } do |f|
      = f.input :first_name, as: :string
      = f.input :last_name, as: :string
      = f.input :email, as: :string
      = f.input :password, as: :string
      = f.input :role
      = f.button :submit, t('helpers.links.save'), class: 'btn-success'
      = link_to t('helpers.links.back'), admin_users_path, class: 'btn btn-default'
